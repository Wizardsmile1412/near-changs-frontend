# API base URL for Near Changs backend (to be created in near-changs-backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# i18n settings (we'll wire these when we add next-intl)
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_SUPPORTED_LOCALES=en,th
